This project monitors a docker container on a remote linode server and restarts either container or the 
whole server

prerequisites:
server on linode with running docker container.
google account with 2fa and app password for email sending.


credentials should exist here, or configure it directly in the script:
ls ~/.aws/





